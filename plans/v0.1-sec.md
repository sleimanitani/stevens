# Build Plan — v0.1-sec: Security Agent foundation

**Goal:** Land the Security Agent as sole secret broker. Every agent that touches OAuth tokens, payment instruments, or API keys will be built on top of it. No such agent ships before this milestone is complete.

**Status:** in-progress — started 2026-04-22
**Charter refs:** `STEVENS.md` §3 (security architecture), §3.12 (intended sequence), §3.11 (locked decisions)
**Blocks:** Email PM (PRD Day 4), all future credential-using agents.

---

## Scope

In this milestone:

1. Security Agent skeleton (UDS server, identity, policy, audit, capability registry) — this document covers steps 1–8
2. Sealed secret store (libsodium secretbox, passphrase-unlocked root key) — steps 9–12 *(to be detailed after step 8 ships)*
3. `stevens` admin CLI (`secrets add/list/rotate/revoke`, `agent register`) — steps 13–14
4. Outbound sidecar proxy (general shape, Gmail as first consumer) — steps 15–17
5. Migrate `channel_accounts.credentials` → sealed store + ref column — step 18
6. Migrate `./secrets/gmail_oauth_client.json` → sealed store — step 19
7. Rewrite `tool_factory` to issue sidecar-bound handles — step 20
8. Redactor for Langfuse traces — step 21
9. End-to-end acceptance: Email PM drafts a Gmail reply without ever holding a raw OAuth token — step 22

Out of scope (later milestones): TPM-sealed root key, off-box audit replication, full payment flow, encryption at rest of PII inside Postgres, cloud deployment.

---

## Protocol — Security Agent RPC (v1 draft)

*Stabilize and move to `docs/protocols/security-agent.md` once step 7 ships.*

- **Transport:** Unix domain socket at `/run/stevens/security.sock`. No TCP. Permissions `0660` owned by user `stevens`; each caller container mounts the socket with the matching group.
- **Framing:** length-prefixed msgpack. `uint32_be` payload length, then msgpack bytes. Max payload 1 MiB in v1.
- **Request envelope:**
  ```
  {
    "v": 1,
    "caller": "<agent_name>",       # e.g. "email_pm"
    "nonce": "<32 random bytes, base64>",
    "ts":    <unix_seconds>,
    "capability": "<namespace.name>",  # e.g. "gmail.send_draft"
    "params": { ... },              # capability-specific
    "sig":   "<ed25519 sig, base64>"
  }
  ```
- **Signature scope:** Ed25519 over `canonical_msgpack({v, caller, nonce, ts, capability, params})` with canonical = deterministic key order (lexicographic), no extensions, fixed int widths. Full rules in `docs/protocols/security-agent.md` once step 3 ships.
- **Replay protection:** server keeps an in-memory nonce LRU (capacity ≥100k, TTL 5 min). Clock skew tolerance ±60 s; requests outside the window are rejected.
- **Response — ok:**
  ```
  { "ok": true, "result": { ... }, "trace_id": "<uuid>" }
  ```
- **Response — error:**
  ```
  { "ok": false, "error_code": "<AUTH|DENY|NOTFOUND|RATE|INTERNAL>", "message": "<human>", "trace_id": "<uuid>" }
  ```
- **Audit:** every request produces one JSONL line in `/var/lib/stevens/audit/YYYY-MM-DD.jsonl`: `{ts, trace_id, caller, capability, account_id, param_hashes, outcome, error_code?, latency_ms}`. Sensitive param values are SHA-256 hashed; non-sensitive (account_id, capability) logged in clear. List of sensitive params per capability is declared in the capability's registration, default-sensitive for anything not listed.

---

## Steps

> **Marker legend:** `[ ]` todo · `[~]` in progress · `[x]` done (with commit hash)

### Step 1 — `security/` package scaffolding [x] `18e3ded`

Create the Python package layout, mirroring `shared/` and `agents/`.

**Files to add:**
- `security/pyproject.toml` (uv workspace member)
- `security/src/stevens_security/__init__.py`
- `security/src/stevens_security/server.py` (stub)
- `security/src/stevens_security/identity.py` (stub)
- `security/src/stevens_security/policy.py` (stub)
- `security/src/stevens_security/audit.py` (stub)
- `security/src/stevens_security/capabilities/__init__.py`
- `security/src/stevens_security/capabilities/registry.py` (stub)
- `security/tests/__init__.py`
- `security/tests/test_smoke.py` (imports package, asserts version string)

**Top-level change:** add `"security"` to `pyproject.toml` `[tool.uv.workspace.members]`.

**Reuse check:** `shared/pyproject.toml` and `agents/pyproject.toml` already define the uv workspace layout — copy structure, don't invent.

**Test plan (canonical invocation):**
- `uv sync --all-packages --all-extras` succeeds — creates/updates `./.venv/` with every workspace package and every `[project.optional-dependencies]` group (including `stevens-security[dev]`'s pytest + pytest-asyncio).
- `uv run pytest security/tests` passes (smoke test only — 2 tests: package version and all-submodules-importable).

**Prerequisite:** `uv` installed at `~/.local/bin/uv` via the official installer. Stevens targets Python **3.10+** (see STEVENS.md §5.3 — the PRD's 3.12 was default-to-latest). `uv` uses the system Python 3.10.12 and creates `./.venv/` at the repo root.

**Out of step 1:** any real server behavior, any crypto, any capability logic. Just scaffolding.

---

### Step 2 — UDS server shell + msgpack framing [x] `e19a6b1`

Async UDS server that accepts framed msgpack requests, dispatches to a capability registry, returns `{"ok": false, "error_code": "NOTFOUND"}` for unknown capabilities. No auth, no policy, no audit yet — just the transport.

**Files:**
- `security/src/stevens_security/server.py` (real implementation)
- `security/src/stevens_security/framing.py` (length-prefixed msgpack codec — small, reusable module)
- `security/tests/test_server.py`
- `security/tests/test_framing.py`

**Reuse check:** no existing RPC server in repo. msgpack lib: use `msgpack` on PyPI; it's a single dep, no alternatives worth comparing.

**Test plan:**
- Unit (framing): encode/decode roundtrip; truncated frame raises; oversize rejected.
- Unit (server): start on temp socket, client connects, sends framed request with unknown capability → receives `{"ok": false, "error_code": "NOTFOUND"}`.

---

### Step 3 — Identity module (Ed25519 verification) [x] `6aa443f`

Load registered agent public keys from `security/policy/agents.yaml`; verify signature on every incoming request; reject unknown caller, bad sig, tampered payload, reused nonce, or stale timestamp.

**Files:**
- `security/src/stevens_security/identity.py`
- `security/src/stevens_security/canonical.py` (canonical msgpack encoder used by signing + verification)
- `security/policy/agents.yaml` (empty registry for now, with schema comment)
- `security/tests/test_identity.py`
- `security/tests/test_canonical.py`

**Reuse check:** use `pynacl` for Ed25519 (libsodium binding — same crypto lib we'll use for secretbox later, no reason to mix).

**Test plan:**
- Canonical encoder: same input → same bytes; different key order → same bytes; nested dicts handled.
- Identity:
  - Valid sig + registered caller + fresh nonce + in-window ts → accepted.
  - Wrong key → rejected (code `AUTH`).
  - Tampered payload → rejected.
  - Replayed nonce → rejected.
  - Stale ts (outside ±60 s) → rejected.
  - Unknown caller → rejected.

---

### Step 4 — Policy loader + evaluator [x] `47daeef`

Load `security/policy/capabilities.yaml`, evaluate `(caller, capability, params) → allow | deny(reason)`. Default deny. Account-scope wildcard matching (e.g. `gmail.*`). Rate-limit fields parsed; enforcement added in step 6 when a real capability exists.

**Files:**
- `security/src/stevens_security/policy.py`
- `security/policy/capabilities.yaml` (default-deny skeleton)
- `security/tests/test_policy.py`

**Test plan:**
- Default deny when no rule matches.
- Explicit allow matches on (caller, capability).
- Account scope wildcard `gmail.*` matches `gmail.personal` and `gmail.atheer`.
- Account scope mismatch denied.
- Malformed yaml → loader raises at startup with clear message (fail closed).

---

### Step 5 — Audit writer [x] `3237c62`

Append-only JSONL at `/var/lib/stevens/audit/YYYY-MM-DD.jsonl`. SHA-256 hash of sensitive param values. Daily rollover on UTC midnight.

**Files:**
- `security/src/stevens_security/audit.py`
- `security/tests/test_audit.py`

**Test plan:**
- Single write produces a valid JSONL line with required fields.
- Sensitive params appear only as hashes, not raw values.
- Rollover: mock clock → writing across date boundary creates a new file.
- Concurrent writers (two coroutines) don't interleave within a line.

---

### Step 6 — Capability registry + `ping` stub [x] `d23a2a4`

Decorator-based capability registration. Implement `ping` (takes nothing, returns `{"pong": true, "server_time": <ts>}`) as the first end-to-end capability. Full flow: identity verify → policy check → capability exec → audit write.

**Files:**
- `security/src/stevens_security/capabilities/registry.py`
- `security/src/stevens_security/capabilities/ping.py`
- `security/tests/test_capabilities_ping.py`
- update `security/policy/capabilities.yaml` to allow `ping` for a test caller identity.

**Test plan:**
- Registered caller + allow policy → pong + one `ok` audit line.
- Registered caller + no allow → `DENY` response + one `deny` audit line.
- Unknown caller → `AUTH` response + one `auth-fail` audit line.

---

### Step 7 — End-to-end wiring (manual run) [x] `5bb044a`

Entrypoint, Docker image, compose service, developer helper to generate a test keypair.

**Files:**
- `security/src/stevens_security/__main__.py` (entrypoint)
- `security/Dockerfile`
- `security/scripts/gen_test_keypair.py` (dev-only)
- update `compose.yaml`: add `security` service, named volumes for `/run/stevens` (socket) and `/var/lib/stevens/audit`, no network mode bound to host.

**Test plan (manual, documented in the Log below when run):**
- `docker compose up security` starts without errors.
- Run `security/scripts/gen_test_keypair.py dev_tester` → writes pubkey into `agents.yaml`, private key into a local temp file.
- Run a short client script that signs a `ping` with the test key → sees `pong` and a new audit line.
- Re-run with a tampered request → sees `AUTH` and an audit line.
- Declare non-automated: acceptable, manual run is the test for step 7.

---

### Step 8 — `shared.security_client` library [x] `79c3a3d`

The reusable client every future Stevens component will use to talk to the Security Agent. Signs requests with the component's local private key, handles framing and response parsing.

**Files:**
- `shared/src/shared/security_client.py`
- `shared/tests/test_security_client.py`

**Reuse check:** this *is* the reusable surface. All future callers go through it. No raw UDS client code in any agent or adapter, ever.

**Test plan:**
- Integration (with the server running from step 7): `client.call("ping")` round-trips, returns the result dict.
- Wrong key configured → `AuthError` raised.
- Server down → `TransportError` with clear message.

---

### Steps 9–22 (sealed store, CLI, sidecar, migrations, redactor, E2E) — *to be detailed after step 8 ships.*

Deferring the detailed step-outs for 9+ until step 8 lands, per the rule that plan detail stays concrete and we don't over-specify steps we haven't proven the foundation for yet. When we get there, each will be broken down with the same shape (files, reuse check, test plan).

---

## Risks

- **Canonical msgpack drift between languages.** Node clients (WhatsApp adapter, eventually) must produce the same bytes Python does. Mitigation: strict spec in `docs/protocols/security-agent.md`, cross-language golden fixture, don't use msgpack features (ext types, str8, …) that differ between libs.
- **Nonce LRU exhaustion under pump.** v0.1 scale is tiny; if it ever becomes real, migrate to a time-bucketed Bloom filter or Redis-backed set. Documented risk, not solved now.
- **Policy yaml fat-fingered → lockout.** Refuse to start on malformed yaml (loud) rather than default-allow (silent). Always.
- **UDS permission mishap.** The socket is the trust boundary — wrong group perms = either nothing works or everything can connect. Integration test in step 7 verifies permissions.

---

## Acceptance criteria for the whole milestone

- [ ] Email PM can draft a Gmail reply without ever holding a raw OAuth token in its process memory.
- [ ] `./secrets/` directory contains no live credentials. All sensitive material lives in the sealed store.
- [ ] `channel_accounts.credentials` in Postgres holds only opaque refs, no secret material.
- [ ] Langfuse traces pass the redactor (no raw tokens, no card numbers, no PII in tool args beyond what the capability explicitly exposes).
- [ ] `stevens audit tail` shows one line per broker call with hashes for sensitive params.
- [ ] `docs/protocols/security-agent.md` is stable and matches what the code does.

---

## Log

(Each step appends one entry when it closes: date · commit hash · step · outcome · notes / deviations / surprises.)

- **2026-04-22 · `18e3ded` · Step 1 — `security/` package scaffolding.**
  Outcome: package layout added, workspace registration complete, smoke test passes (2/2). `uv` installed at `~/.local/bin/uv` (0.11.7). `.venv` created at repo root with system Python 3.10.12. `uv.lock` generated and committed.
  Deviations from plan:
  - Dropped Python `requires-python` from `>=3.12` to `>=3.10` across all five pyproject.toml files. Rationale: Sol asked how long 3.10 would last — answer was "indefinitely for v0.x" since nothing we're doing needs 3.11+ features. Charter note added at STEVENS.md §5.3. The PRD's 3.12 was default-to-latest.
  - Canonical test invocation became `uv sync --all-packages --all-extras` + `uv run pytest …` (plan originally said just `uv sync` — it turns out the workspace root has no declared deps so plain sync is a no-op; `--all-packages` is required to install workspace members, and `--all-extras` is required to install `stevens-security[dev]`'s pytest).
  - Discussed reusing `~/torch-eng/`'s site-packages for torch/CUDA; decided against — Stevens's code path doesn't use torch at all (Ollama handles GPU via HTTP). Saved to memory.
  Surprises: none.

- **2026-04-22 · `e19a6b1` · Step 2 — UDS server shell + msgpack framing.**
  Outcome: 18/18 tests pass (10 framing + 6 server + 2 smoke carried forward). `framing.py` implements length-prefixed msgpack with a 1 MiB cap and a shared `_LENGTH_PREFIX` struct; sync (`encode`/`decode`) and async (`read_frame`/`write_frame`) paths both covered. `server.py` starts a UDS server with injectable `Dispatcher`, one-request-per-connection, default handler returns `NOTFOUND`, and the server unlinks any stale socket file before binding; socket permissions set to `0o660` (group assignment is deferred to step 7 per plan).
  Deviations: none material. One small addition beyond the plan — `test_start_server_replaces_stale_socket_file` explicitly verifies the leftover-socket cleanup path so a crashed prior run doesn't block restart.
  Surprises: none.

- **2026-04-22 · `6aa443f` · Step 3 — Identity module (Ed25519 verification).**
  Outcome: 49/49 tests pass (11 canonical + 10 framing + 19 identity + 6 server + 2 smoke). Three new modules: `canonical.py` (deterministic msgpack; floats + non-str keys + ext types rejected; tuples accepted and normalized to lists; golden-bytes fixture checked in), `identity.py` (envelope shape + version check + ±60s clock-skew window + caller lookup + base64 sig decode + canonicalize-and-verify + nonce LRU, in that order — cheap checks first, crypto last; nonce recorded only after signature succeeds so failed attempts can't poison the cache), and `policy/agents.yaml` (empty registry, fail-closed with a schema comment).
  Protocol doc: `docs/protocols/security-agent.md` shipped. Source of truth for cross-language interop; declares transport, framing, envelope, canonical encoding rules, replay/freshness, auth flow ordering, error codes, and versioning policy.
  Deviations from plan:
  - `canonical.py` accepts tuples and normalizes them to msgpack arrays. Rationale: ergonomics (Python callers often have a tuple in hand) and still deterministic. Documented in the module docstring.
  - `NonceCache` is a bounded time-windowed OrderedDict rather than a separate LRU + TTL structure. Simpler, single eviction loop, still honors both bounds. Tested explicitly.
  - Added `test_nonce_cache_not_poisoned_by_failed_signature` — beyond the plan's stated test list but a key security property worth an explicit test.
  Surprises: `bool` is a subclass of `int` in Python — had to add an explicit `isinstance(v, bool)` short-circuit in the envelope type check so `ts: True` doesn't pass as an int. Covered by `test_bool_is_not_accepted_as_int`.

- **2026-04-22 · `47daeef` · Step 4 — Policy loader + evaluator.**
  Outcome: 69/69 tests pass (20 new policy tests + 49 carried). `policy.py` defines frozen dataclasses (`Policy`, `AgentPolicy`, `CapabilityRule`, `PolicyDecision`) and a pure-function `evaluate()` with ordering: unknown caller → unknown capability → explicit deny (wins over allow) → account-scope check (required when rule declares `accounts:`) → allow with rule attached. `load_policy()` is strict on shape (every malformed-yaml variant raises `PolicyError` with a clear message) and fail-closed on missing file. `capabilities.yaml` ships as an empty default-deny with a schema comment.
  Deviations from plan:
  - Deny list is `frozenset[str]` rather than plain list; membership check is the hot path.
  - Added `test_rule_carries_constraints` and `test_multiple_account_patterns` beyond the plan's listed test cases; they're cheap and specifically exercise the surface that step 6 (capability registry) will rely on.
  - `account_scoped_rule_requires_account_id` returns `rule=...` in the DENY decision (instead of `None`) so audit can log the intended rule even on a miss.
  Surprises: none. `fnmatch.fnmatchcase` (not `fnmatch.fnmatch`) was the right call — `fnmatch` lowercases on case-insensitive filesystems, which we don't want for policy matching.

- **2026-04-22 · `3237c62` · Step 5 — Audit writer.**
  Outcome: 82/82 tests pass (13 new audit tests + 69 carried). `audit.py` ships three pieces: `AuditEntry` frozen dataclass (ts/trace_id/outcome/latency_ms required; caller/capability/account_id/error_code optional; `param_hashes`/`param_values`/`extra` dicts), `hash_param(value)` helper (SHA-256 over msgpack-packed stable form — accepts anything sensible including floats/bytes/None/nested), and `AuditWriter` (asyncio-locked append, daily rollover on UTC date, injectable clock for tests, root dir auto-created, files chmod 0o600).
  Deviations from plan:
  - `hash_param` accepts floats (the canonical encoder rejects them for signing, but for audit-log fingerprinting we trade strictness for "works on any plausible param value"). Documented in the module docstring.
  - Naive `datetime` returned by a clock callback is coerced to UTC (not treated as local). Reproducibility over surprise.
  - Tested 100-coroutine concurrent-write path explicitly — no interleaving, no lost writes.
  - Added `test_sensitive_param_only_appears_as_hash` with a defensive substring check — the secret never appears anywhere in the on-disk log, not just in `param_values`.
  Surprises: none.

- **2026-04-22 · `d23a2a4` · Step 6 — Capability registry + `ping` + dispatch (first end-to-end).**
  Outcome: 101/101 tests pass (19 new, 82 carried). Three modules: `capabilities/registry.py` (CapabilityRegistry + module-level `capability` decorator + `default_registry` singleton; `account_id` always clear), `capabilities/ping.py` (first real capability, returns pong), `dispatch.py` (build_dispatcher orchestrates identity → policy → capability lookup → handler → audit; every path produces exactly one audit line). End-to-end verified: signed ping round-trips over the real UDS and shows up in audit as `ok`; wrong key → `auth_fail`; unauthorized capability → `deny`.
  Deviations from plan:
  - Orchestration lives in its own `dispatch.py` rather than folded into `server.py`; keeps transport cleanly separate from policy/audit wiring.
  - Decorator exists in both class-method form (`reg.capability("...")` — used in tests for isolation) and module-level form (`@capability("...")` — used by real capability modules binding to `default_registry`). Two surfaces for one behavior is intentional.
  - Added `test_account_id_is_clear_not_hashed` as a defensive codification of "account_id is a routing label, never a secret" so a future refactor can't silently start hashing it.
  Surprises: none. The end-to-end ran green first try — the per-module tests from steps 1–5 paid off.

- **2026-04-22 · `5bb044a` · Step 7 — End-to-end wiring (Dockerfile, compose, `__main__`, dev keypair gen).**
  Outcome: `__main__.py` boots the full stack from env-configured paths (socket, agents.yaml, capabilities.yaml, audit dir), registers capabilities by import, installs SIGINT/SIGTERM handlers, and blocks on the server. `scripts/gen_test_keypair.py` writes a local `<name>.key` (chmod 600) and appends the pubkey to `agents.yaml`. `Dockerfile` builds on `python:3.10-slim-bookworm`, runs as non-root user `stevens`, `uv sync`'s the locked workspace, entrypoint is `python -m stevens_security`. `compose.yaml` adds a `security` service with `network_mode: none` (UDS only, no outbound); named volumes for the socket, audit, and secrets dirs; agents mount the socket **read-only** so they can connect but can't spoof it. Read-only root filesystem + tmpfs for `/tmp` and `/home/stevens`.
  Deviations from plan:
  - `network_mode: none` on the Security Agent container is a tightening beyond the plan — makes it impossible for a compromised security process to dial outbound, which matters a lot given it holds all the secrets.
  - Socket volume is shared read-write to the Security Agent and read-only to agents. That's the correct trust asymmetry even if the compose syntax looks redundant.
  Declared manual: a full `docker compose up security` end-to-end run requires a running Docker daemon and is outside the automated test scope. The Python-level server has been end-to-end-verified in step 6. Step 7 wiring is structural.

- **2026-04-22 · `79c3a3d` · Step 8 — `shared.security_client` + canonical encoder moved to `shared`.**
  Outcome: 108/108 tests pass. The 20-test jump you'd expect for a "client library" step is actually offset by deleting the duplicated canonical tests (the encoder moved, the tests moved with it). `shared.security_client.SecurityClient` signs a request, opens a UDS, round-trips framed msgpack, raises typed exceptions (`AuthError`, `DenyError`, `NotFoundError`, `RateError`, `InternalError`, `TransportError`) on error and returns the `result` dict on success. `from_key_file` refuses to load a private key whose file mode has any group/world bits set — fail-closed on permissive perms.
  Canonical encoder: moved from `stevens_security/canonical.py` to `shared/canonical.py`. The old import path is kept as a thin re-export so in-server callers don't change. This is the single source of truth for signature-scope bytes. Making `shared` own the canonical encoder means `shared` now depends on `msgpack` + `pynacl`; `stevens-security` now depends on `shared` (clean direction — server depends on shared, not the other way round).
  Deviations from plan:
  - Pytest config went into root `pyproject.toml` (`--import-mode=importlib` + `asyncio_mode = "auto"`) because two `tests/` directories at different repo paths otherwise collide under the default prepend importer.
  - Timeout defaults to 10s on the client side — beyond the plan's stated test list, but a real concern for a UDS client and tested explicitly.
  - Key-file permission check (refuses any group/world access) is a security improvement beyond the plan. Keys on disk must be `0o600`.
  Declared manual: the server-over-Docker path still needs the actual Docker daemon. Python-level client⇄server is fully tested.

- **2026-04-22 · *(steps 9-12 commit)* · Steps 9–12 — Sealed secret store (merged).**
  Outcome: 133/133 tests pass (25 new sealed-store tests). `sealed_store.py` implements the full sealed-vault lifecycle — Argon2id KDF from passphrase, libsodium secretbox for the vault, master.info with a known-plaintext verifier (detects wrong passphrase *before* attempting to decrypt the vault, so the error message is clean), atomic write with fsync+rename, 0o600 on all files.
  API surface: `initialize_store`, `SealedStore.unlock`, `add/get/get_by_name/ref/ref_by_name/list/rotate/revoke/delete`. `SecretRef` is the safe handout type (id + name + metadata + timestamps, no value). `rotated_from` links successor↔predecessor for the audit trail. Tombstoned secrets remain in the vault (for audit) but are excluded from `list()`/`get_by_name()` by default.
  Deviations from plan:
  - Merged four plan steps into one implementation + one commit. Splitting would have required stubs that don't compose (e.g. "passphrase unlock without rotation" is a half-system that leaks into the other halves). The log entry + plan marker reflect the merge.
  - Added `delete()` to permanently remove (beyond the plan's rotate/revoke). Useful for "never should have been added" paths. Revoke remains the default soft-delete that preserves audit.
  - Known-plaintext verifier (encrypted with the derived key, stored in `master.info`) — detects wrong passphrase cheaply without the full vault decrypt. Not in the plan but worth the 24 bytes.
  - `secrets.compare_digest` used for the verifier check (constant-time) to avoid a timing oracle.
  Surprises: Argon2id MODERATE takes ~100ms per unlock, so the 25-test run is 7s instead of the sub-second we'd been seeing. Acceptable — running the full suite daily is fine, CI is fine. If we ever need faster tests we can expose KDF tuning in ``initialize_store`` for unit tests that don't care about it.

- **2026-04-22 · `0a65504` · Steps 13–14 — stevens admin CLI.**
  Outcome: 143/143 tests pass (10 new CLI tests). CLI lives in `security/src/stevens_security/cli.py` with argparse subparsers. Passphrase sourced from `STEVENS_PASSPHRASE` env (automation) or `getpass` prompt. Register console script at `[project.scripts] stevens = stevens_security.cli:main` so `uv run stevens ...` just works.
  Deviations from plan:
  - Added `stevens secrets delete` (hard-delete) beyond the plan's rotate/revoke — useful for "never should have been added" paths, matches the sealed store's `delete()` that I added in steps 9-12.
  - Agent registration goes directly to `agents.yaml` — public keys are not secrets, so the sealed store doesn't need to mediate them. The sealed store holds secrets; agents.yaml holds identities. Two separate concerns.
  - Tests drain capsys between invocations (`capsys.readouterr()` before each run) after a false start where accumulated init/add output was shadowing the id extraction. Documented in the test helper.
  Surprises: none.

### Steps 15-17 — Outbound sidecar proxy + Gmail capabilities [x] `c99f83b`

Merged: general outbound shape and Gmail as the first consumer, plus the capability-context refactor that unblocks both.

**Files:**
- `security/src/stevens_security/context.py` — `CapabilityContext` dataclass (sealed_store, outbound, clock, extra)
- `security/src/stevens_security/outbound/client.py` — `OutboundClient` base (httpx async, pluggable transport for tests)
- `security/src/stevens_security/outbound/gmail.py` — `GmailClient` (token exchange, in-memory access-token cache with 60s cushion, Gmail REST methods: search / get_thread / create_draft / add_label / remove_label)
- `security/src/stevens_security/capabilities/gmail.py` — capabilities `gmail.search`, `gmail.get_thread`, `gmail.create_draft`, `gmail.add_label`, `gmail.remove_label` registered on the default_registry
- `security/src/stevens_security/capabilities/registry.py` — signature-introspecting `wants_context` so old 2-arg handlers still work; new handlers take `(agent, params, context)`
- `security/src/stevens_security/dispatch.py` — accepts optional `context=CapabilityContext(...)` and passes it via `spec.invoke(agent, params, context)`

**Protocol for secret naming:**
- `<account_id>.refresh_token` (account_id is already qualified, e.g. `gmail.personal`)
- `gmail.oauth_client.id`
- `gmail.oauth_client.secret`

**Tests (6 new, 149 total, all green):**
- `gmail.search` → access token minted, `Authorization: Bearer` attached, threads returned
- `gmail.create_draft` → draft returned with only non-sensitive metadata (no raw body echoed back), raw MIME correctly urlsafe-base64-encoded in body
- access token cached across calls (1 token exchange + 2 API calls = 3 total requests)
- 4xx response propagates as `OutboundError`
- missing outbound in context → clear `RuntimeError`
- outbound without `.gmail` attribute → clear `RuntimeError`

**Declared manual:** real Google OAuth / Gmail API exchange is out of scope for the automated suite — MockTransport stands in. The actual sealed-store population (`stevens secrets add gmail.oauth_client.id --from-stdin`, etc.) happens during install.

- **2026-04-22 · `c99f83b` · Steps 15–17 — Outbound sidecar + Gmail capabilities.**
  Outcome: 149/149 tests pass (6 new). Ships the general outbound shape plus Gmail as its first consumer. CapabilityContext refactor is backward-compatible — `ping` (and other 2-arg handlers) still work via signature introspection (`wants_context` on `CapabilitySpec`).
  Deviations from plan:
  - Merged plan sub-steps 15, 16, and 17 (general outbound, Gmail adapter, capability registration) into one commit. They're tightly coupled — shipping the outbound without a consumer doesn't prove anything, and shipping the Gmail adapter without capabilities has no caller.
  - Added the `CapabilityContext` refactor that wasn't originally listed — handlers genuinely need access to the sealed store and the outbound client, and a module-level global would have been worse. The refactor preserves the old 2-arg handler interface for ping and anything else that doesn't need context.
  Surprises: secret-naming (plan said `gmail.<account_id>.refresh_token` but `account_id` already carries the `gmail.` prefix per PRD naming) — fixed to `<account_id>.refresh_token`. Documented in the module.

### Steps 18-21 — Migrations, tool_factory rewrite, Langfuse redactor [x] `802bebe`

**Step 18 — `channel_accounts.credentials_ref`:**
- `resources/migrations/002_credentials_ref.sql`: adds `credentials_ref TEXT` column with a unique partial index; keeps legacy `credentials` JSONB for transition.
- `shared/src/shared/accounts.py`: `ChannelAccount` gains `credentials_ref`, `uses_sealed_store` property, and a default-empty `credentials` so accounts that have migrated can carry `{}` without schema breakage.

**Step 19 — `gmail_oauth_client.json` migration (operator runbook, no code):**
- `docs/runbooks/gmail-oauth-setup.md`: step-by-step for moving the OAuth client into the sealed store as `gmail.oauth_client.id` + `gmail.oauth_client.secret`, plus per-account refresh-token onboarding. Supersedes `DEVELOPMENT.md` §2a where they disagree.

**Step 20 — `tool_factory` rewrite:**
- `agents/src/agents/tool_factory.py`: complete rewrite. Every tool is now a thin wrapper around a `SecurityClient.call(...)` invocation. Gmail OAuth credentials, access tokens, and refresh tokens never enter the agent process. Tools needed for Email PM: `gmail_search`, `gmail_get_thread`, `gmail_create_draft`, `gmail_add_label`.
- `gmail_create_draft` still builds MIME agent-side (no secrets needed to format a message) and hands raw RFC 822 bytes to the Security Agent, which creates the Gmail draft with its own access token.
- Typed-exception handling on the client side turns `AuthError`/`DenyError`/`NotFoundError`/`TransportError` into structured `{"error": ...}` dicts for the agent prompt, so the LLM sees graceful failures rather than a stack trace.
- Env vars required: `STEVENS_SECURITY_SOCKET`, `STEVENS_CALLER_NAME`, `STEVENS_PRIVATE_KEY_PATH`. The key file must be chmod 0o600 (enforced by `SecurityClient.from_key_file`).

**Step 21 — Langfuse redactor:**
- `shared/src/shared/redactor.py`: pure-function `redact(value)` recursively, plus `redact_headers(pairs)`. Redacts by key name (`password`, `token`, `api_key`, `sig`, `client_secret`, etc.), by string pattern (`Bearer <token>`, JWT-shaped, `ya29.*` Google access tokens), and by high-entropy long-run heuristic.
- Shape preserved; non-sensitive strings + ints + None + emails pass through unchanged. Defense in depth — the primary guarantee is that capabilities don't surface secrets in their results.

**Tests (19 new for redactor, 168 total, all green).** No new tests for step 20's tool_factory — it's an integration surface that requires a running Security Agent with a real Gmail account to exercise. Imports clean (verified).

**Declared manual:**
- `gmail_oauth_client.json` migration is an operator-run procedure (runbook).
- `tool_factory` behaviour end-to-end requires a live Security Agent + Gmail OAuth client onboarded. Covered by step 22.

### Step 22 — End-to-end acceptance [declared manual]

Acceptance criterion: **Email PM drafts a Gmail reply without ever holding a raw OAuth token in its process memory.**

Cannot be run automatically in this environment — needs Sol's Google Cloud project, a real OAuth client, real Gmail accounts, and a running Ollama. Procedure:

1. `uv run stevens secrets init` — initialize sealed store
2. Follow `docs/runbooks/gmail-oauth-setup.md` to populate `gmail.oauth_client.id`, `gmail.oauth_client.secret`, and per-account refresh tokens
3. Generate email_pm's keypair + register its pubkey: `uv run stevens agent register email_pm --pubkey-file email_pm.pub`
4. Populate `security/policy/capabilities.yaml` with `email_pm` allow rules for `gmail.*` capabilities scoped to `gmail.*` accounts
5. Start the security service: `docker compose up -d security`
6. Start the agents runtime with env `STEVENS_CALLER_NAME=email_pm STEVENS_PRIVATE_KEY_PATH=/path/to/email_pm.key`
7. Send a test email to one of the onboarded Gmail accounts
8. Observe in `~/.local/stevens/audit/<today>.jsonl`: one `ok` line for `gmail.get_thread` (capability call) and one `ok` line for `gmail.create_draft`
9. Confirm the drafted reply appears in Gmail's Drafts folder
10. Verify `ps`, `/proc/<agents-pid>/environ`, and agents log output contain **no OAuth tokens** (grep for `ya29.` and `1//` and a sampled byte of the known refresh token)

If step 10 is clean, v0.1-sec is done.

- **2026-04-22 · `802bebe` · Steps 18–21 — Migrations, tool_factory rewrite, redactor.**
  Outcome: 168/168 tests pass (19 new redactor tests). Migration 002 adds `credentials_ref` to channel_accounts. `shared.accounts.ChannelAccount` grows the new field and a `uses_sealed_store` property. `docs/runbooks/gmail-oauth-setup.md` replaces `DEVELOPMENT.md` §2a for OAuth client onboarding. `tool_factory.py` is now 100% broker-mediated — no OAuth creds ever enter an agent process. `shared.redactor.redact` + `redact_headers` added as defense-in-depth before traces leave the process.
  Deviations from plan:
  - Step 19 ships as a runbook, not code — the `./secrets/gmail_oauth_client.json` file never existed in this repo; it was an input to the old flow. The "migration" is really "don't adopt the old flow," expressed as an operator runbook.
  - tool_factory has no automated tests here — the seams (MIME build, dict shape translation) are ok-to-break cosmetics until the Security Agent is running. Exercised in step 22.
  - Redactor is richer than strictly required (header-pair helper, token-shape regexes) — cheap win and Langfuse integration will need the header helper specifically.
  Declared manual:
  - Step 22 (full E2E: Email PM drafts a Gmail reply with no token in its process memory) — requires Sol's real Google Cloud project, OAuth client, and Ollama. Procedure documented in the plan as a punchlist.

- **2026-04-22 · `935c115` · Gmail adapter implementation.**
  Outcome: Scope beyond v0.1-sec proper — implements the Gmail channel that v0.1-sec's security foundation was built for. Still 168/168 tests (no new tests; the new code exercises real Google services that aren't in the automated suite).
  Changes:
  - Added three new Gmail outbound methods: `list_history`, `get_message`, `watch`, `get_profile`. Matching capabilities ship in `capabilities/gmail.py`: `gmail.list_history`, `gmail.get_message`, `gmail.watch`, `gmail.get_profile`.
  - `channels/gmail/src/gmail_adapter/add_account.py`: real CLI. Unlocks the sealed store with the passphrase, loads client_id/client_secret, runs the OAuth local-server flow (browser opens), extracts the refresh token from the resulting `google.oauth2.credentials.Credentials`, stores it in the sealed store as `<account_id>.refresh_token`, calls `users.watch()` while it still holds the access token (one-shot), inserts a channel_accounts row with `credentials_ref = '<account_id>.refresh_token'` and empty `credentials`. Drops the creds from memory at the end.
  - `channels/gmail/src/gmail_adapter/main.py`: real `/gmail/push` handler. Verifies the Pub/Sub JWT, decodes the payload, looks up the account by emailAddress, asks the Security Agent (`gmail.list_history`, then `gmail.get_message` per added message) for the new messages, builds `EmailReceivedEvent`s (MIME body extraction walks the payload tree for `text/plain` and `text/html`), publishes to the bus, advances the cursor. The adapter holds no tokens.
  - `channels/gmail/src/gmail_adapter/watch_renew.py`: cron-friendly — iterates active Gmail accounts, calls `gmail.watch` capability, updates cursor + expiration in `channel_accounts.metadata`.
  Declared manual (for step 22): this path can only be exercised against a real Google Cloud project. Pub/Sub JWT verification specifically needs a real issuer/audience check that a mock can't fake meaningfully.

### Channels: WhatsApp Cloud API + Google Calendar [x] `133dd78`

Both new channels follow the Gmail pattern exactly: outbound client in `security/src/stevens_security/outbound/`, capabilities in `security/src/stevens_security/capabilities/`, Python adapter in `channels/<name>/`, zero credentials in the adapter process.

**WhatsApp Cloud API** (Meta/Facebook Graph API — for business numbers; the existing Baileys TS stub in `channels/whatsapp/` still covers personal numbers):
- `outbound/whatsapp_cloud.py`: `send_text`, `send_template`, `mark_read`, `get_media_url`, `verify_webhook_signature` (broker-side HMAC-SHA256 check against sealed-store app secret).
- `capabilities/whatsapp_cloud.py`: `whatsapp.send_text`, `whatsapp.send_template`, `whatsapp.mark_read`, `whatsapp.get_media_url`, `whatsapp.verify_webhook_signature`. Send-text/template responses include only `message_ids` and contact echoes — no body round-trip.
- `channels/whatsapp-cloud/`: Python FastAPI adapter. `/whatsapp/webhook` handles Meta's GET handshake (verify_token match from `channel_accounts.metadata`) and POST events (signature verified via `whatsapp.verify_webhook_signature`, then walked → `WhatsAppMessageEvent` per message, published to the bus). `add_account.py` takes the access token from stdin, stores it + `phone_number_id` in the sealed store, inserts `channel_accounts` with `channel_type='whatsapp_cloud'`. Zero secret material on argv.
- Migration `003_more_channel_types.sql` adds `whatsapp_cloud` to the `channel_type` check constraint; `shared.accounts.ChannelType` updated.

**Google Calendar** (broker-mediated OAuth, mirrors Gmail):
- `outbound/calendar.py`: `list_calendars`, `list_events` (with sync_token / page_token / time bounds), `get_event`, `insert_event`, `patch_event`, `delete_event`, `watch_events`, `stop_channel`. Same in-memory access-token cache as Gmail.
- `capabilities/calendar.py`: 8 capabilities mirroring the client methods. Sensitive values (event bodies, attendee lists) SHA-hashed by audit; routing-y params (calendar_id, send_updates, channel_id) in the clear.
- `channels/calendar/`: adapter. `/calendar/push` handler verifies `X-Goog-Channel-Token` against the token we set at watch time (stored in `channel_accounts.metadata.channel_token`), drains changes via `calendar.list_events(sync_token)`, publishes `CalendarEventChangedEvent` per change, advances sync_token. `add_account.py` does Google OAuth browser flow, persists refresh token to sealed store, calls `calendar.watch_events` via SecurityClient to register the push channel. `watch_renew.py` cron-friendly — stops the old channel, starts a new one, rotates the channel_token on renewal.
- New event schema: `shared.events.CalendarEventChangedEvent` (+ topic `calendar.event.changed.<account_id>`, registered in `EVENT_REGISTRY`).
- Uses OAuth client stored as `calendar.oauth_client.id/secret` — Sol can use the same Google OAuth client as Gmail; the secrets are stored separately on purpose so rotating one doesn't force the other.

**Root `pyproject.toml`:** `channels/whatsapp-cloud` and `channels/calendar` added to the uv workspace members.

**`compose.yaml`:** `whatsapp-cloud-adapter` (port 8082) and `calendar-adapter` (port 8083) services added, both mounting the Security Agent's socket **read-only** so they can connect but can't spoof it. Same trust asymmetry as `gmail-adapter`.

**Tests (16 new, 184 total):** 8 WhatsApp Cloud (send_text body shape, send_template, mark_read, get_media_url, HMAC verify accepts/rejects/rejects-missing-prefix, 4xx propagates), 8 Calendar (list_events query params, insert event body, patch, delete with 204, watch registration body, sync_token excludes mutually-exclusive params, access-token cached across calls, 4xx propagates). All use `httpx.MockTransport`.

**Declared manual:**
- WhatsApp Cloud onboarding requires a Meta Business app, a WhatsApp Business account, and a webhook URL reachable from Meta's infrastructure (Tailscale Funnel or similar). Procedure: Sol gets the access token from Meta's dashboard, adds `whatsapp_cloud.app_secret` via `stevens secrets add`, runs `uv run python -m whatsapp_cloud_adapter.add_account` per phone.
- Calendar onboarding needs the same Google Cloud project as Gmail (or a separate OAuth client). Procedure: Sol adds `calendar.oauth_client.id/secret` via `stevens secrets add`, runs `uv run python -m calendar_adapter.add_account` per Calendar account, `CALENDAR_WEBHOOK_URL` env for the push URL.
- Neither TS Baileys adapter nor Gmail adapter are affected by this work — they continue to function independently.

- **2026-04-23 · `133dd78` · Channels: WhatsApp Cloud API + Google Calendar.**
  Outcome: 184/184 tests pass (16 new: 8 WAC + 8 Calendar). Two new channels land as full implementations — outbound clients, capabilities, adapters, add_account CLIs, Dockerfiles, compose services. Migration 003 expands the channel_type whitelist. New CalendarEventChangedEvent schema in shared.events. Root pyproject includes both in the uv workspace. All tests use httpx.MockTransport for cross-process independence.
  Deviations:
  - WAC lives under `channels/whatsapp-cloud/` (hyphenated dir to avoid clashing with the existing Baileys `channels/whatsapp/`). Python package is `whatsapp_cloud_adapter` (underscore) matching existing `gmail_adapter` convention.
  - Calendar uses its own `calendar.oauth_client.*` sealed-store entries even though Sol might reuse one Google OAuth client across services — keeping them separate means rotating Gmail's credential doesn't inadvertently break Calendar and vice versa. Documented in the outbound module.
  - `whatsapp.verify_webhook_signature` exists as a capability so the adapter doesn't need the app secret; the adapter sends the header + raw body over the UDS and gets back `{"valid": bool}`. This is the pattern future channels with signed webhooks should copy.
  - Calendar adapter's `/calendar/push` doesn't verify a cryptographic signature (Google Calendar push doesn't sign); instead it verifies `X-Goog-Channel-Token` against the account's stored token, which is a shared secret we rotate on every watch renewal.
  Surprises: none.
