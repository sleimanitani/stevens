# Build Plan — v0.11-plugins

**Goal:** turn external integrations and Mortals into independently installable plugins discovered via Python entry points, with per-plugin manifests declaring mechanism mix, capability scope, secrets, and system deps. Demiurge core ships only the Pantheon, the loader, and the plugin runtime. Hephaestus owns the forge (install/spawn); Hades owns the underworld (uninstall/retire/archive).

**Status:** queued — drafted 2026-05-02. Refined 2026-05-02 with the **powers-not-channels** framing + Hephaestus/Hades as named lifecycle executors (DEMIURGE.md §1.1) + four-mode taxonomy (webhook / listener / polling / request-based). Refined again 2026-05-03 with the full creature taxonomy (Mortal/Beast/Automaton/Angel) + the unified observation feed + the no-Mortal-summon rule + Iris/Zeus distinction. Step 3 split into 3a–3e accordingly.
**Predecessors:** v0.10-bootstrap (the install machinery this milestone extends), v0.10.1-rename (Stevens → Demiurge), v0.10.2-pantheon-expansion (Hephaestus + Hades declared in DEMIURGE.md §1.1).
**Charter refs:** DEMIURGE.md §2 Principles 12 (Pantheon/Mortals) and 13 (Plugins, not monoliths). Architecture: `docs/architecture/pantheon.md`, `docs/architecture/agent-isolation.md` §§3.6 (capability grant width) and 4 (Mortal lifecycle).

---

## Why this milestone exists

Today the channel adapters (gmail, calendar, whatsapp-cloud, signal) live in `channels/<name>/` directories inside the main repo, and the Mortals (email_pm, installer) live in `agents/src/agents/<name>/`. Adding a new external integration or hiring a Mortal is a code change in core — even though their interfaces are already stable enough to live behind a manifest. This:

- Couples the release cadence of every integration to the core release.
- Means third parties can't ship integrations without touching core.
- Bloats core install footprint (you install the WhatsApp Cloud adapter even if you only use Gmail).
- Makes "uninstall integration X" non-trivial — there's no reverse of `git add`.
- Same problem for Mortals: there's no clean `demiurge hire retire trip-planner` if `trip-planner` is in-tree code.
- The "channel" abstraction also doesn't fit emerging integrations cleanly. A stock-price API isn't a channel — there's nothing inbound. An image generator isn't a channel either. A polling RSS reader is somewhere between. Whatever we call it, an external-world integration is shaped by *which mechanisms it uses* (webhook, listener, polling, request-based), not by whether it's "communication."

The fix is the standard Python plugin pattern + a unified vocabulary. Each integration is a **power** — a pip-installable package with a manifest declaring its mechanism mix, capabilities, secrets, and bootstrap. Mortals are similar but task-scoped.

---

## Vocabulary

- **Power** — any external-world integration. Replaces the channel-only framing. A power's manifest declares one or more **modes**:

| Mode | Direction | Trigger | Example |
|---|---|---|---|
| **Webhook** | inbound | remote pushes to us (HTTPS endpoint) | Gmail Pub/Sub, WhatsApp Cloud, Stripe |
| **Listener** | inbound | we hold a long-lived outbound connection | IMAP IDLE, Signal websocket, MQTT |
| **Polling** | inbound | we hit them on a schedule | RSS feed, stock price |
| **Request-based** | outbound | agent calls on demand | image generator, web search |

A power can combine modes (Gmail = webhook + request-based; Signal = listener + request-based; image generator = request-based-only; RSS = polling-only).

- **Pantheon members** stay where they are: Enkidu, Arachne, Sphinx, Janus shipped; Hephaestus + Hades planned for this milestone; Mnemosyne + Iris planned for v0.12. Pantheon members are not powers — they're the substrate powers and Mortals depend on.
- **Mortals** stay as defined: task-scoped agents, can be hired and retired, declare their dependencies on powers + capabilities in their manifest.

---

## Scope

1. **Plugin manifest format** — `plugin.yaml` (or `pyproject.toml` table) declaring: name, kind (`power` | `mortal`), modes (powers only — list of webhook/listener/polling/request-based), runtime block (per-mode runtime details), capabilities required, powers required (for Mortals), secrets required, system deps, version, source URL, ownership.
2. **Entry-point discovery** — Python entry points under `demiurge.powers` and `demiurge.mortals` namespaces. Demiurge scans these on startup; whatever's installed is what's available. The hardcoded `_CHANNELS` list in `channels_list.py` becomes a *registry of available* plugins (catalog), separate from *installed* (entry-point scan).
3. **`demiurge powers`** subcommands: `list` (installed), `registry` (curated catalog of known powers), `install <name>` (pip install + Hephaestus bootstrap hook), `uninstall <name>` (Hades teardown — revoke caps, drop schema, pip uninstall), `show <name>` (manifest + state). The legacy `demiurge channels …` aliases remain functional but are documented as deprecated.
4. **`demiurge hire`** subcommands: `list`, `registry`, `install <name>` (install plugin Mortal — Hephaestus forge), `spawn <spec>` (ad-hoc Mortal from inline spec — Hephaestus forge), `show <id>`, `retire <id>` (Hades archive), `pause <id>` / `resume <id>`.
5. **Per-Mortal Postgres schema** — each Mortal at hire time gets `mortal_<id>` schema. Migrations scoped to the Mortal run in that schema. On retire (Hades): the schema is archived (default) or dropped (with `--drop-data`).
6. **Capability grants visible per Mortal + per power** — `demiurge hire show <id>` and `demiurge powers show <name>` print the manifest's declared capability scope, the actually-granted scope (after Sol's approval), and the audit-tail tally of calls actually made. Lets Sol see drift between declared and used.
7. **Migrate existing in-tree code** to the plugin shape:
   - `channels/gmail/` → `demiurge-power-gmail` plugin (lives in `plugins/demiurge-power-gmail/` for now; separate repo in a later cleanup pass).
   - Same for `channels/calendar/`, `channels/whatsapp-cloud/`, `channels/signal/`.
   - `agents/src/agents/email_pm/` → `demiurge-mortal-email-pm` plugin.
   - `agents/src/agents/installer/` → `demiurge-mortal-installer` plugin.
   - The `channels/`, `agents/` directories are emptied or removed; core only ships the Pantheon (`security/`, `agents/src/agents/{web,pdf,janus}/` if those Pantheon members live in the agents tree, plus future memory + interface).
8. **Hephaestus** (`forge` Pantheon member) — concrete implementation of the create-side lifecycle executor. Reads a manifest, registers capabilities with Enkidu, generates the runtime artifact per the manifest's `runtime:` block (no unit for request-based-only powers; `.service` for webhook/listener; `.service`+`.timer` for polling), wires bus subscriptions, records the apotheosis hook for any future promotion. Owns Apotheosis, Succession (forge side), Binding (forge side).
9. **Hades** (`underworld` Pantheon member) — concrete implementation of the end-side lifecycle executor. Tears down the runtime, revokes capabilities, archives the audit trail and last state to `archived_mortal_<id>_<ts>` schema (or `archived_power_<name>_<ts>`), frees secrets back to Enkidu. Owns Fading, Exile, Ragnarök, Succession (sever side), Binding (archival side).
10. **Plugin runtime supervisor** — `demiurge_runtime` daemon (a systemd user unit). Scans plugin entry points at startup, starts each reactive (webhook/listener/polling) plugin as a subprocess per its `runtime:` block, supervises with restart-on-failure backoff. Replaces the hardcoded 6-unit catalog from v0.10 step 3 — Hephaestus generates the units from manifests; runtime supervises them.

Out of scope:
- Public PyPI publishing of the per-power plugins. They live in-tree under `plugins/` for v0.11; PyPI publishing happens when there's a real third-party need.
- A federated plugin marketplace. The `demiurge powers registry` command reads from a single curated YAML in core for now.
- Sandboxing plugins beyond the existing capability/policy model. A plugin that gets approved capabilities is trusted with those capabilities; the security boundary is at Enkidu, not at the plugin process boundary.

---

## Plugin manifest spec (draft)

```yaml
# plugin.yaml — committed to the plugin's repo
name: gmail
kind: power                         # or: mortal
display_name: Gmail
version: 1.0.0
source: https://github.com/<org>/demiurge-power-gmail
maintainer: Sol

# Modes (powers only) — which mechanism(s) this power uses.
# Hephaestus reads this to know what runtime artifact to generate.
modes:
  - webhook
  - request-based

# Per-mode runtime details (powers only).
# Each declared mode needs a corresponding runtime block.
runtime:
  webhook:
    path: /gmail/push                 # URL path on the FastAPI mux
    port: 8080                        # local port the listener binds to
    handler: demiurge_power_gmail.adapter:webhook_handler
  # listener:
  #   command: demiurge_power_signal.adapter:run_listener
  #   restart: on-failure
  # polling:
  #   command: demiurge_power_rss.fetch:run_once
  #   interval: 1h
  # (request-based has no runtime block — it just registers capabilities)

# Capabilities this plugin needs to function.
# For powers: capabilities the adapter calls.
# For Mortals: capabilities the agent's policy block declares.
capabilities:
  - gmail.send
  - gmail.read
  - gmail.draft

# Powers a Mortal needs (Mortal-only).
# Sol must have these powers installed before the Mortal can be hired.
powers:                             # Mortal-only field (was: channels)
  - gmail

# Sealed-store secrets the plugin expects, with prompts.
# Bootstrap hook checks these are present and runs the right onboarding.
secrets:
  - name: gmail.oauth_client.id
    prompt: "Google OAuth client ID"
    onboard_via: "demiurge wizard google"
  - name: gmail.oauth_client.secret
    prompt: "Google OAuth client secret"
    onboard_via: "demiurge wizard google"

# System dependencies (apt packages, brew formulas, …).
# If missing at install time, the installer Mortal proposes a plan to install.
system_deps:
  apt: []
  brew: []

# Bootstrap hook — Python entry point invoked on `demiurge powers install <name>`.
# Receives a context object with sealed-store + db handles via Enkidu.
bootstrap: demiurge_power_gmail.bootstrap:install
```

The `pyproject.toml` of the plugin declares the entry-point side:

```toml
[project.entry-points."demiurge.powers"]
gmail = "demiurge_power_gmail:manifest"
```

The runtime calls `manifest()` → `Manifest` object → which knows where the `plugin.yaml` lives.

---

## Steps

> `[ ]` todo · `[~]` in progress · `[x]` done.

### Step 1 — manifest schema + parser `[x]` (2026-05-02)

`shared/src/shared/plugins/manifest.py`. Pydantic model for `plugin.yaml`. `load_manifest(package_name) → Manifest`. Validation:

- `kind` is `power` or `mortal`
- `modes` is required for `power`, forbidden for `mortal`; each declared mode in `[webhook, listener, polling, request-based]`
- `runtime` block has a key for each declared *reactive* mode (webhook/listener/polling); request-based skips
- `capabilities` exist in the Pantheon's registry (validate against a hardcoded list for now; v0.12 + plugin caps will refine)
- `secrets` follow `<power>.<account>.<key>` or `<power>.<key>` shape
- `powers` (Mortal-only) lists known power names

**Test:** load valid gmail.yaml + a Mortal yaml; cover each invalid case (wrong mode, missing runtime block for a declared reactive mode, capability typos, etc.).

**Outcome:** new `shared/src/shared/plugins/` subpackage. `manifest.py` (~330 LOC) defines the full schema:

- `Mode` enum (webhook/listener/polling/request-based) + `REACTIVE_MODES` constant
- `WebhookRuntime` (path/port/handler), `ListenerRuntime` (command/restart), `PollingRuntime` (command/interval) — each with `extra="forbid"` so typos are caught
- `RuntimeBlock` (optional webhook/listener/polling sub-blocks)
- `SecretSpec` (sealed-store key with structural validation: `<power>.<account>.<key>` or `<power>.<key>`)
- `SystemDeps` (apt/brew lists)
- `Manifest` top-level with cross-field validators: name regex, mode/runtime alignment per kind, request-based-only must have no runtime block, `powers` field is Mortal-only, `bootstrap` required for powers, capability shape `<namespace>.<verb>`

Loaders: `load_manifest_from_text(yaml_str) → Manifest` and `load_manifest_from_yaml(path) → Manifest`. Both raise `ManifestError` (single exception class) for parse + validation failures, with one short line per failed field.

Re-exports from `shared.plugins`: `Manifest`, `ManifestError`, `Mode`, `PowerKind`, `RuntimeBlock`, `SecretSpec`, plus the two loaders. `load_manifest(package_name)` from the plan is renamed to `load_manifest_from_yaml(path)` since the package-name lookup is step 2's discovery concern, not step 1's parsing concern — adjusted accordingly.

22 unit tests covering: 5 valid manifests (gmail webhook+request, signal listener+request, RSS polling-only, image-gen request-only, email_pm Mortal); 12 invalid cases (wrong kind/modes mix, unknown mode, duplicate modes, missing/extra runtime block, request-based-only-with-runtime, uppercase name, malformed capabilities, malformed secret names, powers field on a power, missing bootstrap on a power, unknown extra top-level field, malformed YAML). Full suite: 715 passed, 2 skipped (+22 unit tests).

---

## Architectural decisions locked 2026-05-03 (apply throughout)

These came out of the Hephaestus design discussion and bind the rest of v0.11.

1. **Four creature kinds** — Mortal (full agency), Beast (model-driven, no agency), Automaton (deterministic, no LLM), Angel (god-extension, opaque). Future fifth: Prophet, reserved. See DEMIURGE.md §1.1 + docs/architecture/gods.md.
2. **No journal, no scratchpad, no diary in Creature code.** Universal tools shrink to `think(text)` (speak aloud for the audit angel) + `mortal.return(result)` only. Memory recall is a blessed tool `tools.memory.recall(query)` (Mnemosyne stub before v0.13).
3. **Single observation feed per Creature** at `~/.local/state/demiurge/creatures/<id>/events.jsonl`, mode 0640, owned by Enkidu's uid. Stable envelope: `creature_id`, `event_id` (UUIDv7), `ts` (ISO8601 µs), `kind`, `correlation_id`, `data`. Single time source, single ID namespace. Cross-angel join is `JOIN ON event_id`.
4. **Audit angel mandatory.** Enkidu always commissions an audit angel for every Creature. v0.11 ships it as in-process Enkidu code projecting the feed into the existing tamper-evident audit log. v0.13 promotes it to a real out-of-process angel alongside Mnemosyne's memory angel.
5. **Mortals do not summon.** No `mortal.summon` tool. No `can_summon` manifest field. Creature creation is a god-level act. v0.11: only Sol spawns (via `demiurge hire`). v0.12+: Mortals request from Zeus (`zeus.request_spawn`-shaped blessed tool); Iris is in the path *only* when the request originates from Sol.
6. **Permissions immutable.** A Creature's blessings are set at forge time and don't mutate for its lifetime. Change = retire + re-forge. Static authority graph; no dynamic permission lending. Mortals working *with* each other within their existing capabilities (bus events, calling existing Beasts/Automatons) is fine.
7. **Iris ≠ Zeus ≠ Demiurge.** Iris is Sol's personal UI (preferences, modality, notification routing). Zeus is the chairman (multi-god dispatch + judgment). Demiurge is the substrate (bootstrap, supervisor, lifecycle). All three planned post-v0.11.
8. **Cross-god access:** reads allowed (any god can read another's substrate when investigating); writes forbidden (each god is the only writer to its own substrate; integrity follows from single-writer rule).

### Step 2 — entry-point discovery `[x]` (2026-05-03)

`shared/src/shared/plugins/discovery.py`. `discover(kind: Literal['power','mortal']) → list[InstalledPlugin]`. Wraps `importlib.metadata.entry_points`. Returns name + manifest + entry callable.

**Test:** install a fake plugin in a `tmp_path` venv, discover it. Test both kinds; test that a malformed entry point raises a clean error.

**Outcome:** new `shared/src/shared/plugins/discovery.py` (~210 LOC). Public surface:

- `POWERS_GROUP = "demiurge.powers"`, `MORTALS_GROUP = "demiurge.mortals"` — the two entry-point group names plugins register under.
- `InstalledPlugin` (frozen dataclass): `name`, `kind`, `manifest`, `dist_name`, `dist_version`, `entry_point_value`.
- `DiscoveryError` (frozen dataclass): `group`, `name`, `dist_name`, `entry_point_value`, `error`.
- `DiscoveryResult` dataclass (`plugins: list[InstalledPlugin]`, `errors: list[DiscoveryError]`, `.names()` helper).
- `discover(kind: Literal["power","mortal"]) → DiscoveryResult` — fault-tolerant: each entry point examined independently; broken plugins go in `errors[]` with a short reason, working plugins go in `plugins[]`. Catches: `ManifestError` (wrong shape), generic `Exception` (broken imports), kind mismatch (Mortal manifest registered under powers group), entry-point-key vs manifest-name disagreement.
- `load_manifest_for_package(package_name)` convenience using `importlib.resources` — lets a plugin's `manifest()` callable return `load_manifest_for_package("demiurge_power_gmail")` if it ships a `plugin.yaml` data file.

Entry-point load contract (documented in module docstring): the target must be either a `Manifest` instance or a zero-arg callable returning a `Manifest`. Anything else surfaces as a `DiscoveryError` with a clear "expected Manifest" message.

16 unit tests cover: happy paths (callable returning Manifest, bare Manifest instance, multiple powers, separate group for Mortals, empty group); error paths (broken import, non-Manifest target, callable returning wrong type, kind mismatch, name mismatch, partial failure with one good + one broken plugin); dist-lookup edge case (entry point with no `.dist`); `load_manifest_for_package` (real package layout in `tmp_path` with valid yaml, missing yaml, unimportable package). Tests use a duck-typed `MagicMock` instead of trying to construct real `EntryPoint` objects (their constructor signature varies across Python versions). Full suite: 731 passed, 2 skipped (+16 unit tests).

### Step 3 — Hephaestus (forge) module — split into 3a–3e

Hephaestus is the keystone of v0.11: he forges every Creature kind, gathers blessings from each relevant god, and attaches the mandatory audit angel + any optional angels other gods commission. Big enough to split.

#### Step 3a — Creature interfaces in `shared.creatures` `[ ]`

Pure types + a few stub impls. No god integration yet. Defines what every Creature (Mortal, Beast, Automaton) looks like, plus the Angel ABC and the universal-tools surface.

`shared/src/shared/creatures/`:
- `base.py` — `Creature` (root ABC), `Mortal(Creature)`, `Beast(Creature)`, `Automaton(Creature)`, `Angel(Creature)`.
- `context.py` — `CreatureContext` base + `MortalContext`, `BeastContext`, `AutomatonContext`. Frozen dataclasses. Mortals get `llm`, `tools`, `memory`, `bus`, `audit`, `logger`. Beasts get `model_handle`, `audit`, `logger`. Automatons get `bus`, `audit`, `logger` only.
- `tools.py` — `ToolRegistry`, `Blessing`, `Denial` types (consumer-side; the dispatcher is 3b/3c).
- `universal.py` — `think(text)` and `mortal.return(result)` only. Both write to the observation feed.
- `feed.py` — observation feed writer (envelope schema, UUIDv7 event_id minting, atomic appends to the per-Creature events.jsonl). The supervisor calls into this; Creatures don't see it directly.

**Test:** ABC subclassing works, contexts are immutable, universal tools land in the feed with the right envelope, feed writer is concurrency-safe (parallel appends preserve all events).

#### Step 3b — Blessing protocol + GodlyBlessing `[ ]`

Generic bless-request shape. `GodlyBlessing` Protocol that every god implements: `async def bless(*, creature_id, request) → Blessing | Denial`. `commission_angel(creature_id) → AngelSpec | None` for angel commissioning.

**Test:** in-memory mock gods that bless or deny; blessing token shape is structured (creature_id, scope, expiration, signed by Enkidu eventually); cross-god dispatch fan-out completes in parallel.

#### Step 3c — Tool routing + dispatch `[ ]`

Prefix → god mapping (`gmail.*` → Enkidu; `web.*` → Arachne; `pdf.*` → Sphinx; `browser.*` → Janus; `memory.*` → Mnemosyne stub; `say.*` → Iris stub; `zeus.*` → Zeus stub). `forge_blessings(manifest) → BlessedToolRegistry`. Wires Enkidu's existing capability system as the first concrete `GodlyBlessing` impl. Arachne, Sphinx, Janus get thin wrappers delegating to their existing per-call permission machinery. Mnemosyne/Iris/Zeus get stubs that return ok blessings for capabilities prefixed `memory.recall`/etc but otherwise deny.

**Test:** end-to-end blessing collection for a multi-god manifest (gmail + web + pdf); failure modes (one god denies → forge fails); tool wrapper validates blessing token on every call.

#### Step 3d — `forge_power(manifest)` `[ ]`

`security/src/demiurge/pantheon/hephaestus/forge.py`. Build the runtime artifact for a power per its `modes:` + `runtime:` block:
- webhook → uvicorn unit on declared port + path
- listener → long-running `python -m <command>` unit
- polling → service unit + systemd timer
- request-based-only → no unit; just register the capabilities

Wire bus subscriptions per the manifest. Run the bootstrap hook. Return `ForgeResult`. Idempotent: re-forging an already-forged power is a no-op.

**Test:** unit tests against a mocked `bootstrap.systemd`. Integration test (gated on $DATABASE_URL) for the registry write.

#### Step 3e — `forge_mortal / forge_beast / forge_automaton` + audit angel placeholder + scheduler Automaton `[ ]`

The full Creature forge. For Mortals:
1. Provision agent identity (re-use existing `provision_agent`).
2. Mint `creature_id` (UUIDv7).
3. Create `mortal_<id>` Postgres schema (per-Mortal namespace).
4. Compose `MortalContext`: blessed ToolRegistry from 3c, LLM stub (Iris-mediated post-v0.12; thin wrapper over Ollama for now), memory stub (Mnemosyne-mediated post-v0.13; per-schema postgres for now), bus handle scoped to declared subscriptions, audit logger.
5. Wire the observation feed writer into the supervisor.
6. **Attach Enkidu audit angel placeholder** — in-process projection of the feed into today's audit log. Refactor existing audit-log writer to live behind the `Angel` protocol so v0.13 can promote it to out-of-process without changing what it produces.
7. Hand off to the supervisor.

For Beasts: same but no `mortal_<id>` schema, no LLM handle (just a model handle), shorter `BeastContext`.

For Automatons: same but no LLM, no memory, just a bus handle + audit. No agency.

Plus: ship a real **`scheduler` Automaton** as the proof-of-Automaton example. It tick-fires `creature.tick.<id>` events on a manifest-declared cron, used by polling-mode powers and any Mortal that wants periodic wake-ups.

**Test:** end-to-end forge on a fake mortal/beast/automaton manifest; the audit angel placeholder produces audit-log lines indistinguishable from today's; idempotent re-forge; the scheduler Automaton fires on cadence in a tmp_path test.

### Step 4 — Hades (underworld) module `[ ]`

`security/src/demiurge/pantheon/hades.py`. The underworld:
- `archive_power(name) → ArchiveResult` — revokes capabilities via Enkidu, removes the systemd unit, archives the audit trail to a per-power tarball under `~/.local/state/demiurge/archive/`.
- `archive_mortal(id) → ArchiveResult` — same plus rename `mortal_<id>` schema → `archived_mortal_<id>_<ts>`. With `drop_data=True`, drops the schema instead.
- `fade_pantheon_member(name)`, `exile_pantheon_member(name, reason)`, `ragnarok(...)` — heavier transitions on Pantheon members; for v0.11 these are stubs that print "manual intervention" instructions, since we don't have a Pantheon member that's currently fading.

**Test:** unit + integration. Verify `archive_mortal` followed by re-`forge_mortal` with the same id is idempotent (the archive doesn't block a re-spawn).

### Step 5 — `demiurge powers` CLI `[ ]`

Replace the hardcoded `_CHANNELS` list with discovery output. Subcommands:
- `list` — installed powers (via discovery)
- `registry` — known catalog (`resources/powers_registry.yaml`)
- `install <name>` — pip install + Hephaestus.forge_power
- `uninstall <name>` — Hades.archive_power + pip uninstall
- `show <name>` — manifest + capability scope + audit tally

Plus deprecated aliases: `demiurge channels list/registry/install/uninstall/show` → call the powers subcommand with a deprecation warning.

**Test:** `powers list` against a venv with no plugins → empty. After install of `demiurge-power-gmail`, lists it; the right systemd unit got generated; uninstall reverts cleanly.

### Step 6 — `demiurge hire` CLI `[ ]`

New top-level subcommand. `list`, `registry`, `install`, `spawn`, `show`, `retire`, `pause`, `resume`. Per-Mortal-id state in a `mortals` table (created by a new migration). `retire` calls Hades.archive_mortal.

**Test:** end-to-end on a fake Mortal: install → spawn → show → retire — all the state transitions land in the `mortals` table and Enkidu's policy. After retire, the schema is archived; the agent identity is tombstoned.

### Step 7 — runtime supervisor `[ ]`

`demiurge_runtime` package. systemd user unit (one unit, supervises N plugins — replaces the hardcoded 6-unit catalog from v0.10). At startup: discover entry points; for each reactive plugin, start its declared runtime command as a subprocess; supervise with restart-on-failure backoff. Per-plugin logs go to `~/.local/state/demiurge/logs/<name>.log`.

For polling-mode plugins specifically: instead of a long-running subprocess, the runtime calls the plugin's `polling.command` on a schedule (driven by an in-process scheduler, since it's lighter than systemd timers and lets us share state).

**Test:** start runtime with two fake plugins (one webhook, one polling); kill one; verify restart. Stop runtime; verify all plugins die cleanly.

### Step 8 — migrate existing channels → powers `[ ]`

Move each of `channels/{gmail,calendar,whatsapp-cloud,signal}/` into `plugins/demiurge-power-<name>/` with a `pyproject.toml` declaring entry points and a `plugin.yaml` (with the right `modes:` and `runtime:` blocks). Each gets its own version, README, and bootstrap hook (the bootstrap hook codifies the existing `add_account.py` flow).

**Test:** for each power, the pre-v0.11 onboarding flow still works through `demiurge powers install <name>` instead of the old direct `python -m channel_adapter.add_account`.

### Step 9 — migrate existing Mortals `[ ]`

Same shape for `email_pm` and `installer`: extract to `plugins/demiurge-mortal-<name>/`, declare manifest, register entry point. The old `agents/src/agents/{email_pm,installer}/` directories get removed.

**Test:** `demiurge hire install email-pm` reproduces the current Email PM behavior end-to-end.

### Step 10 — runbook + doc overhaul `[ ]`

Rewrite per-channel runbooks (now per-power) to lead with `demiurge powers install <name>`. Add a "Hires" section to the master runbook covering `demiurge hire install / spawn`. STATUS.md + DEMIURGE.md callouts where "channel" is used as a forward-looking concept switch to "power."

**Test:** install Demiurge fresh + install one power + hire one Mortal following only the runbooks.

### Step 11 — acceptance `[ ]`

End-to-end on a clean install:

1. `uv run demiurge bootstrap` (from v0.10).
2. `uv run demiurge secrets init`.
3. `uv run demiurge powers install gmail` → pip-installs the gmail plugin, runs Hephaestus.forge_power, prompts for OAuth.
4. `uv run demiurge hire install email-pm` → installs the Mortal plugin, presents capability scope for approval, Hephaestus.forge_mortal spawns the supervised process.
5. Send a test email. Email PM picks it up and drafts a reply through Enkidu.
6. `uv run demiurge hire retire email-pm` → Hades.archive_mortal succeeds; the schema becomes `archived_mortal_email_pm_<ts>`.
7. `uv run demiurge powers uninstall gmail` → Hades.archive_power → removes plugin, revokes caps, removes runtime unit. Demiurge is back to a clean Pantheon-only state.

**Acceptance gate:** under 10 minutes from a clean v0.10 install to a working Email PM through plugins, with no in-tree channel/agent code touched.

---

## Open questions

- **Mortal manifests on hire — parameterization.** Trip-planner takes `--for-trip "Tokyo Sept 2026"` at hire time, distinct from another trip-planner instance for a different trip. Yes, almost certainly worth supporting — confirm parameter shape (Pydantic model declared in the manifest's `params:` field, validated against the inline spec at `spawn` time?) before locking it in.
- **Plugin → plugin dependencies.** A Mortal manifest's `powers:` list + a preflight check at hire time that errors with "install <power> first" — sufficient for v0.11. Plugin → plugin Mortal dependencies (one Mortal needs another Mortal already running) — defer to v0.11.x if it comes up.
- **Plugin signing / chain of trust.** Out of scope for v0.11. Sol installs from his own trusted sources; signing matters when there's a marketplace.
- **Per-power webhook port allocation.** Today each adapter binds its own port (8080, 8082, 8083). Plugins should declare the port they want; Hephaestus needs to detect collisions and either remap or error. Worth deciding if v0.11 ships a single FastAPI mux that hosts all webhook routes (cleaner) vs preserving per-plugin port binding (simpler). Lean: single mux, but confirm before locking.
