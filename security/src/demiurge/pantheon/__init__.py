"""Pantheon implementations — the gods themselves.

v0.11 introduces the `hephaestus` (forge) and `hades` (underworld)
subpackages. Existing gods (Enkidu, Arachne, Sphinx, Janus) live in
their original locations under `demiurge/`; this package adds the
new ones plus thin adapter shims that present each god under a
uniform `GodlyBlessing` interface for Hephaestus's forge time.
"""
