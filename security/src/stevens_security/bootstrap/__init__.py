"""First-time setup machinery for Stevens (v0.10).

Submodules:
- ``migrate``: apply SQL migrations against $DATABASE_URL via psycopg.

Future submodules (v0.10 step 2+): ``postgres``, ``systemd``, ``preflight``.
"""
